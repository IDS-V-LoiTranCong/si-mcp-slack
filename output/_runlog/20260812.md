# Daily Report Runlog — 12/08/2026

| Dự án | Channel | Trạng thái | Tỉ lệ báo cáo | Ghi chú |
|---|---|---|---|---|
| — (toàn cục) | — | Lỗi | — | Dừng toàn bộ automation ở Bước 0: tool `slack_get_upload_identity` không khả dụng trên MCP server `slack-upload` (chỉ có `slack_upload_markdown`), không khoá được UPLOADER_USER_ID → không thể nhận diện report đã upload và tránh gửi trùng. |
| NDT TEST | #pj-ndt-test-report | Bỏ qua | — | Không xử lý do dừng toàn cục ở Bước 0. |

## Chi tiết

- **TODAY_VN:** 2026-08-12 (Thứ Tư) — không phải cuối tuần, không nằm trong `config/holidays.yml`.
- **Thời điểm chạy:** ~11:10 (UTC+7).
- **Hành động cần thiết:** Bổ sung/enable tool `slack_get_upload_identity` trên MCP `slack-upload`, trả về `user_id` bắt đầu bằng `U`.
